const numbers = [1, 5, 6, 4, 15];
/*
map and for each do the same thing but the difference is map return result as array where as for each dont return anything. Below an example given.

*/

const results = numbers.forEach(n => console.log(n));